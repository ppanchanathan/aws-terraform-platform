name: Platform Policy Check

on:
  pull_request:
    paths:
      - "platform/**"
      - "bootstrap/**"

permissions:
  contents: read

jobs:
  policy-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Placeholder Policy Check
        run: |
          echo "Add Checkov, tfsec, or conftest here later."
