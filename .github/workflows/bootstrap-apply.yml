name: Bootstrap Apply

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Bootstrap Notice
        run: |
          echo "Bootstrap should normally be run locally."
          echo "This workflow is intentionally manual only."
