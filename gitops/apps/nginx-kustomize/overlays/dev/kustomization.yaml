apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nginx-dev
namePrefix: dev-

resources:
  - ../../base
  - hpa.yaml

patches:
  - path: deployment-patch.yaml

#commonLabels:
#  environment: dev

labels:
  - pairs:
      environment: dev