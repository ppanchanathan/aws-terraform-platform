apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nginx-prod
namePrefix: prod-

resources:
  - ../../base
  - hpa.yaml

patches:
  - path: deployment-patch.yaml

#commonLabels:
#  environment: prod

labels:
  - pairs:
      environment: prod