apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rollout.yaml
  - service.yaml
  - service-preview.yaml

#commonLabels:
#  app.kubernetes.io/managed-by: argocd

labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd